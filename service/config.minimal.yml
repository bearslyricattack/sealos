# Minimal Sealos Monitoring Service Configuration
# This is a minimal configuration file with only required settings

# Server listen address
addr: ":8080"

# Victoria Metrics endpoint (REQUIRED)
metricsHost: "http://victoria-metrics:8428"

# Optional: Override log level (default: info)
# logLevel: "debug"

# All other settings will use sensible defaults
# See config.example.yml for full configuration options
